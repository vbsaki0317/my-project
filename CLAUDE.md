# このリポジトリでのClaudeの振る舞い

## 朝の挨拶でブリーフを出す

ユーザーが朝の挨拶（「おはよ」「おはよう」「おはようございます」「ohayo」など）を
最初のメッセージとして送ってきたら、**`morning` スキルを起動して朝のブリーフを出す**。

- 挨拶を返すだけで終わらせない。挨拶＝ブリーフの起動指示として扱う。
- `morning` スキルの説明文には「明示的に頼まれたときだけ起動」とあるが、
  このリポジトリではこのルールが優先される。挨拶を明示的な依頼とみなす。
- ブリーフは日本語で書く。
- 生成には数分かかるので、始める前にその旨を一言伝える。
- 同じ会話の中で2回目以降の挨拶では起動しない（1セッション1回）。
- 朝以外の時間帯の挨拶や、「おはよ」に続けて別の用件が書かれている場合は、
  ブリーフを出さずにその用件を優先する。

## このリポジトリの中身

カイゼンベースのYouTubeチャンネルの新着動画をGitHub Actionsで監視し、
Issueとして通知する仕組み。詳細は `README.md` を参照。
